apiVersion: v1
kind: ServiceAccount
metadata:
  name: game-catalog
  namespace: ps-store
  labels:
    app.kubernetes.io/name: game-catalog
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: ps-store
automountServiceAccountToken: false
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: game-catalog
  namespace: ps-store
  labels:
    app.kubernetes.io/name: game-catalog
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: ps-store
data:
  PORT: "8001"
  ORDER_SERVICE_URL: "http://order-service:8003"
  USER_SERVICE_URL: "http://user-service:8002"
  OTEL_SERVICE_NAME: "game-catalog-service"
  OTEL_EXPORTER_ZIPKIN_ENDPOINT: "http://zipkin.observability.svc.cluster.local:9411/api/v2/spans"
---
apiVersion: v1
kind: Secret
metadata:
  name: game-catalog
  namespace: ps-store
  labels:
    app.kubernetes.io/name: game-catalog
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: ps-store
  annotations:
    kubernetes.io/description: "Service-specific secrets. Replace with sealed-secrets/ESO in production."
type: Opaque
stringData:
  DATABASE_URL: "postgresql://postgres:ChangeMe-Use-SealedSecrets-Or-ESO@postgres:5432/catalog_db"
  JWT_SECRET: "ps-store-jwt-secret-key-change-in-production"
---
apiVersion: v1
kind: Service
metadata:
  name: game-catalog
  namespace: ps-store
  labels:
    app.kubernetes.io/name: game-catalog
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: ps-store
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: game-catalog
    app.kubernetes.io/component: backend
  ports:
    - name: http
      port: 8001
      targetPort: http
      protocol: TCP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: game-catalog
  namespace: ps-store
  labels:
    app.kubernetes.io/name: game-catalog
    app.kubernetes.io/instance: game-catalog
    app.kubernetes.io/version: "1.0.0"
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: ps-store
  annotations:
    kubernetes.io/description: "FastAPI catalog service backed by Postgres catalog_db."
    kubernetes.io/change-cause: "Initial Kubernetes deployment"
spec:
  replicas: 2
  revisionHistoryLimit: 5
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app.kubernetes.io/name: game-catalog
      app.kubernetes.io/component: backend
  template:
    metadata:
      labels:
        app.kubernetes.io/name: game-catalog
        app.kubernetes.io/instance: game-catalog
        app.kubernetes.io/version: "1.0.0"
        app.kubernetes.io/component: backend
        app.kubernetes.io/part-of: ps-store
    spec:
      serviceAccountName: game-catalog
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: game-catalog
      containers:
        - name: game-catalog
          image: ps-store/game-catalog:1.0.0
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8001
              protocol: TCP
          envFrom:
            - configMapRef:
                name: game-catalog
            - secretRef:
                name: game-catalog
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: "1"
              memory: 512Mi
          startupProbe:
            httpGet:
              path: /health
              port: http
            failureThreshold: 30
            periodSeconds: 5
          readinessProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 3
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir:
            sizeLimit: 64Mi
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: game-catalog
  namespace: ps-store
  labels:
    app.kubernetes.io/name: game-catalog
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: ps-store
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: game-catalog
      app.kubernetes.io/component: backend
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: game-catalog
  namespace: ps-store
  labels:
    app.kubernetes.io/name: game-catalog
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: ps-store
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: game-catalog
  minReplicas: 2
  maxReplicas: 6
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
  behavior:
    scaleDown:
      stabilizationWindowSeconds: 300
    scaleUp:
      stabilizationWindowSeconds: 30
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: game-catalog
  namespace: ps-store
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: game-catalog
      app.kubernetes.io/component: backend
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - podSelector:
            matchExpressions:
              - key: app.kubernetes.io/name
                operator: In
                values: ["frontend", "user-service", "order-service"]
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: ingress-nginx
      ports:
        - protocol: TCP
          port: 8001
  egress:
    - to:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: postgres
      ports:
        - protocol: TCP
          port: 5432
    - to:
        - podSelector:
            matchExpressions:
              - key: app.kubernetes.io/name
                operator: In
                values: ["user-service", "order-service"]
      ports:
        - protocol: TCP
          port: 8002
        - protocol: TCP
          port: 8003
