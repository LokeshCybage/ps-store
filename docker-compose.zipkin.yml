# Optional: run Zipkin for local distributed tracing.
# Usage: docker compose -f docker-compose.zipkin.yml up -d
# UI: http://localhost:9411

services:
  zipkin:
    image: openzipkin/zipkin:latest
    ports:
      - "9411:9411"
