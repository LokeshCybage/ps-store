server {
    listen 8080;
    root /usr/share/nginx/html;
    index index.html;

    # Never serve index.html for missing hashed bundles (breaks the SPA with a blank page).
    location /assets/ {
        try_files $uri =404;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
